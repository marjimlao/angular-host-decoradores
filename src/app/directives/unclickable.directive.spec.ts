import { UnclickableDirective } from './unclickable.directive';

describe('UnclickableDirective', () => {
  it('should create an instance', () => {
    const directive = new UnclickableDirective();
    expect(directive).toBeTruthy();
  });
});
